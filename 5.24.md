####  LLM结构设计：

- LLM nav和LLM router合并.节省token的同时更好让LLM把握整体探索节奏.

- 细致分类给出LLM全局任务
  - 先登录，再进入主页面，再以某种方式探索。根据不同app做对应任务流程。
  - 根据问卷设置各种任务，比如访问设置页面看那几个设置相关的问题。访问pp和TOS。
  - 每个任务设置步长。这样不会卡在某个任务里很久导致拖延时间。
- UTG想办法传给LLM交互,让他对目前动作流程有更好的认识。
  - 完整的UTG可能太大了，需要压缩一下？再传？？或者是每个UI用LLM概括的话语来描述，就不传原图了？？
  - 这一部分用来代替传入的action history.起到更好的效果,不止告诉LLM之前干了啥,还告诉LLM更多信息.
- return action貌似没用??因为可以通过UTG图中边来移动。可以考虑一次性传之前那张UI和目前UI，然后去分析。之前UI作为上下文，告诉LLM之前是啥界面点了啥，到了现在界面，LLM还需要找到办法返回之前页面（某个按钮，返回键啥的）。

#### dismiss设计:

- LLM判断dismiss的步骤感觉可以优化一下..可以当做一个正常UI状态处理，但是只让LLM给出一个候选动作。并在UTG图中标识该节点为dismiss节点。
  - 这个候选动作视我们目前的任务而定。如果弹窗不是由于我们交互产生的，就关闭，如果和交互相关，是具体情况解决。

#### 动态页面处理

1. xml可信情况处理:根据xml树，识别其中不同节点

2. xml不可信：ocr结果以及图像相似度做处理。

#### LLM填写问卷能力提升

---

[UI Transition Interactive - 20260531_sudoku_depth_steps_schemafix_10min](file:///F:/workplace/framework/traces/20260531_sudoku_depth_steps_schemafix_10min/index.html)

里面UI14 | xml:65f2cc6c46733c4a408d4b420caa8dd1
没正确找到点击按钮.

