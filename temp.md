1. - **一、整体原则**

     ```
     当前正在探索哪个页面，就优先把这个页面的候选动作消耗完。 候选动作只要成功执行，就认为这个入口动作已经消费过，后面不要在同一个页面重复点。 动作执行后，先做轻量页面变化检测，不立刻完整 capture_and_process。 如果到了已知页面，就优先尝试沿 UTG 状态图回到刚才正在探索的页面。 如果 UTG 回源页面失败，就检查当前实际落在哪个页面，然后在实际页面继续探索。 返回动作暂时保留，但每个返回动作要记录实际结果，避免反复执行无效返回动作。
     ```
   
     **二、候选动作执行逻辑**
   
     ```
     当前页面记为 A 当前页面快照记为 A_snap 如果 A 页面还没有候选动作计划：    等待大语言模型返回 navigation 结果    保存 A 的候选动作列表    保存 A 的返回动作列表 从 A 的候选动作列表里，找到第一个还没有消费过的动作，记为动作 X 如果找不到动作 X：    说明 A 的普通候选动作已经都消费完了    进入“返回动作逻辑”    结束本轮 准备执行动作 X 执行动作 X 如果动作 X 执行失败：    把动作 X 标记为“已消费，执行失败”    继续留在 A    下一轮继续找 A 的下一个候选动作    结束本轮 如果动作 X 执行成功：    立刻把动作 X 标记为“已消费，执行成功”    注意：这里不等待 X 通向的页面全部探索完    因为这个标记的含义是“这个入口动作已经点过了”
     ```

     动作成功后继续：

     ```
     先检查当前前台包名 如果当前前台包不是目标 APP：    记录一次外部跳转    保存外部页面截图/XML/包名/activity    把目标 APP 拉回前台     拉回后，对 A_snap 做一次轻量页面变化检测     如果检测结果是“仍然是 A”：        当前页面仍然设为 A        继续探索 A 的剩余候选动作        结束本轮     如果检测结果是“变到了已知页面 K”：        进入“已知页面处理逻辑”        结束本轮     如果检测结果是“变到了未知页面”：        执行完整 capture_and_process        得到新页面 N        记录 A 通过动作 X 到达 N        当前页面切换为 N        给 N 安排大语言模型分析        结束本轮
  ```
   
  如果还在目标 APP 内：
   
  ```
     对 A_snap 做一次轻量页面变化检测 如果检测结果是“页面没有变化”：    记录 A 通过动作 X 仍然停留在 A    当前页面继续设为 A    继续探索 A 的剩余候选动作    结束本轮 如果检测结果是“变到了已知页面 K”：    记录 A 通过动作 X 到达 K    进入“已知页面处理逻辑”    结束本轮 如果检测结果是“变到了未知页面”：    执行完整 capture_and_process    得到新页面 N    记录 A 通过动作 X 到达 N    当前页面切换为 N    给 N 安排大语言模型分析    结束本轮
     ```
   
  **三、已知页面处理逻辑**
   
  ```
     已知页面记为 K 刚才正在探索的源页面记为 A 如果 K 就是 A：    当前页面继续设为 A    继续探索 A 的剩余候选动作    结束 如果 K 不是 A：    优先尝试沿 UTG 状态图，从 K 回到 A     记录日志：        准备 UTG 回源        起点是 K        目标是 A        原因是“候选动作进入了已知页面，尝试回到源页面继续探索”
     ```
   
  执行 UTG 回源：
   
  ```
     尝试沿 UTG 状态图，从 K 执行动作回到 A 如果 UTG 回源成功：    当前页面恢复为 A    继续探索 A 的剩余候选动作    记录日志：UTG 回源成功    结束 如果 UTG 回源失败：    注意：此时当前实际页面不一定还是 K    需要重新判断当前实际页面在哪里     先做轻量页面变化检测     如果能判断当前是某个已知页面 R：        当前页面切换为 R        在 R 上继续探索        记录日志：UTG 回源失败，实际落点是 R        结束     如果判断当前是未知页面：        执行完整 capture_and_process        得到新页面 R        当前页面切换为 R        给 R 安排大语言模型分析        记录日志：UTG 回源失败，实际落点是新页面 R        结束     如果当前前台包已经不在目标 APP：        记录外部跳转        拉回目标 APP        再判断拉回后的实际页面        结束
  ```
   
     (这一段应该是这样逻辑:这一段我觉得应该是这个逻辑:

     如果 UTG 回源失败：

     ​    注意：此时当前实际页面不一定还是 K
   
     ​    需要重新判断当前实际页面在哪里

     

     ​	先判断前台包,如果不是的话拉回.

     ​    先做轻量页面变化检测

     

     ​    如果能判断当前是某个已知页面 R：

     ​        当前页面切换为 R

     ​        在 R 上继续探索
   
  ​        记录日志：UTG 回源失败，实际落点是 R
   
     ​        结束
   
  
   
  ​    如果判断当前是未知页面R：
   
  ​    	记录回溯失败,
   
  ​    	重启app什么的
   
     )

   
     **四、返回动作逻辑**
   
  ```
     当前页面 A 的普通候选动作已经全部消费完 读取 A 的返回动作列表 如果 A 没有返回动作：    执行系统 BACK    然后做轻量页面变化检测    根据检测结果切换页面    结束本轮
  ```
   
     如果有返回动作：
   
  ```
     按照大语言模型给出的顺序，依次查看返回动作 对于每个返回动作 R：     如果 R 之前已经尝试过，并且结果是：        执行失败        页面没变化        进入新页面        进入非祖先已知页面        外部跳转     那么这次跳过 R，不重复执行它     如果 R 没有尝试过：        执行 R
  ```
   
  (就是这里说的祖先节点是啥意思,就是他的父节点是吗?按照DFS栈来说,当前节点探索完了退栈之后的话,那不就应该是他的父亲节点嘛.对吧..?假如回到了祖先节点,就好比说是父节点的父节点的话怎么处理比较好?是切换DFS栈还是说沿UTG移动到父节点再探索???)
   
     执行返回动作 R 后：

     ```
     如果 R 执行失败：    标记 R 的结果为“执行失败”    尝试下一个返回动作    不切换页面 如果 R 执行成功：    对 A_snap 做轻量页面变化检测
     ```
   
     根据检测结果：
   
     ```
     如果检测结果是“页面没有变化”：    标记 R 的结果为“执行成功但页面没变化”    认为这个返回动作无效    尝试下一个返回动作 如果检测结果是“变到了已知页面 K”：    记录 A 通过返回动作 R 到达 K     如果 K 是 A 的祖先页面：        标记 R 的结果为“成功返回祖先页面”        当前页面切换为 K        修正 DFS 栈到 K        不再尝试 A 的其他返回动作        结束本轮     如果 K 不是 A 的祖先页面：        标记 R 的结果为“进入已知非祖先页面”        当前页面切换为 K        在 K 上继续探索        不再尝试 A 的其他返回动作        结束本轮 如果检测结果是“变到了未知页面”：    执行完整 capture_and_process    得到新页面 N     记录 A 通过返回动作 R 到达 N    标记 R 的结果为“进入新页面”    当前页面切换为 N    给 N 安排大语言模型分析    不再尝试 A 的其他返回动作    结束本轮 如果检测结果是“跳到了外部 APP”：    标记 R 的结果为“外部跳转”    保存外部页面信息    拉回目标 APP    然后判断拉回后的实际页面    不再尝试 A 的其他返回动作    结束本轮
     ```
   
     如果所有返回动作都试过了，但都没有正常返回：
   
     ```
     执行系统 BACK 作为兜底 如果系统 BACK 成功：    做轻量页面变化检测    根据结果切换页面 如果系统 BACK 也失败或页面没变化：    标记当前页面为“返回耗尽”    后续可以考虑 recovery、重启 replay、或者全局切换
     ```
   
     (这里我觉得应该优先看看有没有UTG图中从当前节点到他父亲节点的边,有的话尝试沿图移动一下试试.没有的话再考虑重启)
   
     **五、返回动作结果标记**
     每个页面维护一份返回动作结果记录：
   
     ```
     页面 A:    返回动作 R1:        是否尝试过：是        执行是否成功：是        实际结果：成功返回祖先页面 / 页面没变化 / 进入新页面 / 进入非祖先已知页面 / 外部跳转 / 执行失败        到达页面：某个 state_sig，如果有        记录时间：当前 step_id     返回动作 R2:        是否尝试过：否
     ```
   
     这个标记的作用：
   
     ```
     避免同一个无效返回动作反复执行。 如果返回动作 R1 会进入新页面，那么以后再回到 A，A 的普通候选动作都消耗完后，不再重复执行 R1，而是尝试 R2。 如果某个返回动作确实能回到祖先页面，那么后续可以认为它是可信返回动作。
     ```
   
     **六、轻量页面变化检测逻辑**
     轻量检测只判断页面有没有变、变到哪里，不做 three-tools。
   
     输入：
   
     ```
     预期页面快照 expected_snap
     ```
   
     输出：
   
     ```
     页面没变化 变到已知页面 变到未知页面 跳到外部 APP 轻量抓取失败
     ```
   
     详细流程：
   
     ```
     第一步：抓当前轻量 snapshot 轻量 snapshot 只包括：    当前 XML    当前截图    当前前台包名    当前 activity    设备信息中必要的少量字段 不做：    OCR    UIED    图标分类    template 检测    uist 后处理    vid_map 生成
     ```
   
     第二步：检查前台包名
   
     ```
     如果当前前台包名不是目标 APP，也不是允许的系统弹窗包：    返回结果：外部 APP    附带当前包名、activity、raw snapshot
     ```
   
     第三步：看 expected_snap 的 XML 是否可信
   
     ```
     如果 expected_snap 里记录 xml_reliable = true：    走 XML 可信分支 否则：    走截图 phash 分支
     ```
   
     XML 可信分支：
   
     ```
     取 expected_snap 里的旧 XML hash 计算当前轻量 XML 的新 XML hash 如果新旧 XML hash 一样：    返回结果：页面没变化 如果新旧 XML hash 不一样：    在已知页面缓存里查找有没有相同 XML hash 的页面     如果找到了已知页面 K：        返回结果：变到已知页面 K     如果没找到：        返回结果：变到未知页面        附带当前 raw snapshot
     ```
   
     XML 不可信分支：
   
     ```
     取 expected_snap 里的旧截图 phash 计算当前轻量截图的新 phash 计算新旧 phash 相似度 如果相似度大于等于阈值：    返回结果：页面没变化 如果相似度低于阈值：    在已知页面缓存里找 phash 最相似的页面     如果找到已知页面 K，且相似度大于等于阈值：        返回结果：变到已知页面 K     如果没有找到足够相似的已知页面：        返回结果：变到未知页面        附带当前 raw snapshot
     ```
   
     轻量抓取失败：
   
     ```
     如果轻量 snapshot 抓取失败：    返回结果：轻量抓取失败 上层逻辑可以决定：    重试    或者直接完整 capture_and_process
     ```
   
     **七、完整 capture_and_process 什么时候执行**
     只在这些情况执行：
   
     ```
     轻量检测认为页面变到了未知页面 轻量检测失败且必须恢复状态 初始化入口页面 确实需要为新 UI 生成 uist、vid_map、LLM 输入
     ```
   
     不应该在这些情况执行：
   
  ```
     动作后页面没变化 动作后回到已知页面 UTG restore 成功回到已知页面 返回动作回到已知祖先页面
     ```

     **八、最终主循环摘要**
   
     ```
     抓入口页面完整 snap 安排入口页面 LLM 分析 循环开始：     如果当前页面有 overlay：        执行 overlay dismiss        动作后做轻量检测        根据结果更新当前页面        继续循环     如果当前页面还没有候选动作计划：        等待 LLM navigation        冻结候选动作列表和返回动作列表     如果当前页面还有未消费候选动作：        执行下一个候选动作        立刻标记候选动作已消费        动作后做轻量检测         如果没变化：            继续当前页面         如果到已知页面：            优先 UTG 回源页面            回源成功则继续源页面            回源失败则在实际页面继续         如果到未知页面：            完整 capture_and_process            进入新页面         如果到外部 APP：            记录外部页面            拉回 APP            判断实际页面        继续循环     如果当前页面候选动作都消费完：        执行返回动作逻辑        继续循环
     ```
   
     (这里我也有个问题,就是假如是执行某个动作之后,然后得到的新页面是一个dismiss页面,那处理起来会不会有问题?因为就算是有这个弹窗,它可能也是一个状态吧。比如说它是点了某个按钮之后,它出来一个弹窗,这种情况的话,我觉得它应该是一个合法的状态,它可能就是一个UI,它可能不能严格意义上算一个弹窗。就是反正我是这么想的,就是如果啊,比如说我现在点了一个按钮,是进入商店页面的这个语意的一个按钮,然后它出来了一个弹窗,问,你是否要进入商店页面,然后那按照大原模型的这个策略,那可能这种弹窗它都会优先点no,否,或者是点叉,那它点了否之后,那就进不到那个商店页面了。然后而且这个动作相当于就是白执行了。这就我就在想,这个这个这个情况会不会有问题?)
     这个版本应该更贴近你的想法：
   
  ```
     当前页面优先探索完。 候选动作点过就消费。 已知页面优先尝试回源。 回源失败就看实际落点继续。 返回动作保留但要记录结果，不重复点无效返回动作。 轻量 drift 和完整 capture_and_process 拆开。
  ```

(先这样吧,就是我们打算解决的问题,你就呃就是我提出来可能有问题的地方,你都整理一下, 然后我们已经解决了的问题,感觉就就无关没关系了,然后有的问题就可以就先不解决了,我们先做一个简化一点版本的嘛,不解决了就不解决了,反正让它整个流程能跑起来就行,因为真实测的时候可能会遇到各种各样的问题,就是让它最简单的能跑起来,我觉得就是我们当务之急了。嗯 这样吧,然后就是你把整个,整个这个流程里刚才我们也讨论了很多嘛,刚才我提出的可能有问题的点,然后而且还是我们还没有解决的这些点,你也都整理出来列出来,然后给我看一下。)