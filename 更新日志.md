# 更新日志

## 2026-05-04

### 本次代码功能更新（相对 `origin/ruogan..HEAD`）

- 重写 `LLM2` 导航系统提示词（`gpt_cls.py`）：
  - 从概念性说明升级为字段级/流程级约束；
  - 强化 `overlay_kind` 判定策略；
  - 强化 `candidate_actions` 与 `return_method/return_actions` 约束；
  - 目标是提升动作提议的稳定性与可执行性。
- LLM2判断页面是否有继续探索的必要。页面无探索必要就不继续分析。
- 修改paddle_cls代码，解决paddle版本适配问题。

## 2026-05-13

### capture_and_process 调试链路与三工具分支更新

- `ui_cls.py` 三工具后处理分支调整：
  - 新增 `BaseUI._assign_ids_all_nodes(uist)`，用于给 UI 树全部节点分配 `id` 并写入 `vid_map`。
  - `BaseUI.post_process_ui_three_tools_debug(...)` 改为使用全量 `id` 分配（`vid_map = _assign_ids_all_nodes(...)`）。
  - 目的：在 `xml_reliable=False` 的三工具调试场景中，方便完整核对融合后的全部元素。
  - 说明：`xml_reliable=True` 仍保持原有 `post_process_ui(...)` 逻辑，不受本次改动影响。

- `workflow.py` 调试捕获函数更新：
  - 新增/完善 `_capture_and_process_debug(self, timeout=..., debug=False, raw=None, xml_raw=None)`：
    - 支持注入本地 `raw/xml_raw` 做离线调试；
    - 保留与主链路一致的状态签名、缓存、裁剪、元信息打点流程；
    - 在调试函数内部按 `xml_reliable` 分流：
      - `True` -> `post_process_ui(...)`
      - `False` -> `post_process_ui_three_tools_debug(...)`
  - 修复调试入参判断逻辑，避免布尔与位运算混用导致的潜在异常。

- 新增本地离线调试脚本：
  - `mytest2/debug_capture_and_process.py`
  - 特点：
    - 使用代码内固定配置（图片/XML/输出目录），不依赖命令行参数；
    - 调用 `_capture_and_process_debug(...)`；
    - 输出 `snap/uist/vid_map/overlay/summary` 等调试产物；
    - 附加 `uist_total_nodes` 与 `vid_map_count` 对齐校验，便于确认全量映射是否生效。

### 当前接入状态

- 主流程 `_capture_and_process(...)` 已完全切换到与 `_capture_and_process_debug(...)` 相同的核心实现。
- 当前实际行为：
  - `xml_reliable=True`：仍走 `post_process_ui(...)`（原可信 XML 分支逻辑保持不变）。
  - `xml_reliable=False`：走 `post_process_ui_three_tools_debug(...)`（三工具融合 + 全量 `vid_map`）。

## 2026-05-16

### 调试链路阶段性更新

- *换用了新的api,注意接口url..

- `ui_cls.py` 三工具后处理分支更新：
  - 将 `BaseUI._POSTPROCESS_BUNDLE_VERSION` 从 `v1` 更新为 `v2`，避免复用旧 snapshot cache。
  - 新增 `BaseUI._force_all_nodes_clickable(uist)`：
    - 输入：三工具融合后的 `uist`。
    - 输出：原地修改节点，并返回修改节点数量。
    - 功能：把所有节点统一设置为 `clickable=True`、`enabled=True`。
  - 在 `BaseUI.post_process_ui_three_tools_debug(...)` 中，生成 `vid_map` 前先调用 `_force_all_nodes_clickable(...)`。
  - 当前效果：
    - `xml_reliable=False` 时，`snap["uist"]` 与 `snap["vid_map"]` 中所有节点都会被视作可点击。
    - `xml_reliable=True` 的可信 XML 分支不受影响。
  - 目的：避免三工具/OCR/UIED 生成的视觉按钮因为 `clickable=False` 被 LLM 导航阶段忽略。

- `gpt_cls.py` LLM Navigation 输出结构缩减：
  - 精简 `ActionStep`：
    - 保留 `action`、`element_id`、`text`、`priority`、`reasoning`。
    - 移除 `x`、`y`、`bbox`、`probe_grid`，不再允许 LLM 自行输出截图区域点击。
  - 精简 `ActionCandidate`：
    - 保留 `actions`、`score`。
    - 移除候选动作级 `return_method`、`return_actions`、`tags`。
  - 精简 `NavigationProposal`：
    - 保留 `state_sig`、`page_summary`、`overlay_kind`、`overlay_reason`、`overlay_dismiss_actions`、`candidate_actions`。
    - 新增 `page_return_actions`，用于表达页面级返回/关闭/切回动作。
    - 移除 `ui_view`、`ui_type`、`page_tags`、`tag_evidence`、`key_interactables`、`workflow_hints`、`exhausted*`、全局 `return_method/return_actions`、`why_these_actions`。
  - 同步修改 `_NAV_SYSTEM` 与 `_NAV_ROUTER_SYSTEM`：
    - 明确 `candidate_actions` 只放探索动作。
    - 明确 `page_return_actions` 只放页面级返回/关闭/切回动作。
    - 要求两者不要重复使用同一个 `element_id`。
    - 要求没有可见返回控件时 `page_return_actions=[]`，不编造返回动作。
    - 要求不输出坐标或 bbox。
  - 注意：当前精简后的 Navigation schema 主要服务调试脚本；完整主流程如果仍依赖旧字段，需要后续单独适配。

- `test_debug/test_llm_navigation.py` 与 `test_debug/test_llm_navigation_router.py` 更新：
  - 新增 `ui_digest.json` 输出：
    - `test_llm_navigation.py` 保存与 `GPTClient.propose_navigation(...)` 一致的 `_compact_digest(..., limit=240)`。
    - `test_llm_navigation_router.py` 保存与 `GPTClient.propose_navigation_and_router(...)` 一致的 `_compact_digest(..., limit=260)`。
    - 目的：方便核对 LLM 实际看到的 UI 元素、`clickable` 状态与标签内容。
  - 调整 LLM 动作 overlay 图：
    - 蓝色 `C*`：`candidate_actions`，表示探索候选动作。
    - 红色 `O*`：`overlay_dismiss_actions`，表示弹窗/遮挡层关闭动作。
    - 棕色/橙色 `PR*`：`page_return_actions`，表示页面级返回/关闭/切回动作。
  - 不再绘制旧的 `candidate.return_actions` 或全局 `return_actions`。
  - 删除测试脚本中基于 `bbox/x/y` 的画框 fallback；现在 overlay 只根据 `element_id -> vid_map` 定位。

## 2026-05-18

### 主流程 DFS / 回退 / 防循环更新

- `workflow.py` 主探索流程调整：
  - 引入页面级返回动作 `page_return_actions`，用于表达返回、关闭、切回父页面等页面级动作。
  - 候选动作 `candidate_actions` 仍只表示探索动作，不再和返回动作混用。
  - 页面普通候选动作执行后立即记录已尝试/已探索，避免“等待整条分支探索完才标记”导致重复点击。
  - 对已知状态优先复用状态缓存与已冻结动作，减少重复提交 LLM 与重复完整 `capture_and_process`。
  - 新增 return exhausted 相关处理：当页面候选动作耗尽、返回动作无效、UTG 父路径不可达、BACK 无变化时，将该状态标记为返回耗尽，避免重复执行同一套返回逻辑。
  - 新增 `recovery_route_exhausted` 终止保护：当 return exhausted 后尝试重启/replay 到全局 frontier 失败时，不再继续完整抓取同一页面，而是停止本次运行。

- 轻量级 drift / 已知状态处理调整：
  - 保留轻量 drift 作为动作执行后的优先判断路径。
  - 当轻量判断命中已知状态时，优先切换到已知状态缓存，而不是直接强制完整 `capture_and_process`。
  - 将完整状态归并阈值与轻量 drift 阈值拆开：完整状态归并更严格，降低 XML 不可信页面误归并风险。

### Overlay 处理更新

- `workflow.py` overlay dismiss 流程调整：
  - `overlay_kind=dismiss` 时优先执行 `overlay_dismiss_actions`。
  - 如果某个 dismiss 动作执行失败，继续尝试下一个 dismiss 动作。
  - 如果某个 dismiss 动作执行后页面无变化，也继续尝试下一个 dismiss 动作。

  - 当 LLM 错把明显的关闭/取消/稍后/跳过动作放到 `candidate_actions` 且 `overlay_dismiss_actions` 为空时，保留兼容 fallback，从候选动作中筛选可疑关闭动作。
  - 同一 overlay family 的 recovery 次数新增上限 `overlay_recovery_limit_per_family=3`。
  - 超过上限后设置 `overlay_recovery_exhausted`，主流程以 `overlay_recovery_exhausted` 作为停止原因结束，避免错误关闭按钮或不可关闭弹窗导致无限 recovery/restart。

- `gpt_cls.py` Navigation prompt 更新：
  - 明确 `overlay_kind=dismiss` 时 `candidate_actions=[]`。
  - 要求所有关闭、拒绝、稍后、跳过、继续类动作放入 `overlay_dismiss_actions`，并按安全程度排序。
  - 要求不要把全屏根节点或普通容器节点当作关闭按钮，除非它是唯一明确可点的关闭/继续控件。
  - 增加登录/社交登录约束：除非任务明确需要登录，或没有其他可探索的非登录入口，否则不要选择 login / sign-in / social-login 动作。
  - 合并版 `NavigationRouter` prompt 同步加入上述规则。

### 可视化与批量测试更新

- `main.py` 新增 `--auto-visualize-interactive`：
  - 主流程结束后可自动调用 `visualize_run_interactive.py` 生成交互式 UI 状态图。
  - 可视化失败只记录 warning，不影响主流程退出。

- `visualize_run_interactive.py` 交互式 HTML 更新：
  - 保留节点点击后查看节点详情的交互。
  - 右侧新增更适合人工调试的全局动作时间线。
  - 时间线重点展示状态发现、动作执行、状态跳转、外部跳转、recovery/restart 等关键事件。
  - 目标是结合左侧 UTG 图快速判断“在哪个状态执行了哪个动作，以及动作结果是什么”。

- `test_debug/run_batch_apps.py` 新增批量抽样测试脚本：
  - 从 `metadata_downloaded_apps.csv` 中按固定随机种子抽样。
  - 默认抽样 10 个 APP，每个 APP 运行 300 秒。
  - 根据 `category` 字段映射问卷类型：游戏类 -> `games`，社交/通信类 -> `social_apps`，其他 -> `others`。
  - 输出 `sampled_apps.csv`、`batch_summary.json`、`batch_summary.md`，便于快速检查批量运行是否出现死循环或严重错误。

### Metadata CSV 兼容更新

- `main.py` metadata 预分析兼容 `metadata_downloaded_apps.csv`：
  - 查找 APP 行时同时支持 `appId`、`APID`、`package`、`package_name`、`packageName`、`app_id`。
  - 字段归一化时保留旧格式字段，同时兼容新 CSV 中的 `description` 和 `category`。
  - 新 CSV 缺失的 `descriptionHTML`、`summary`、`contentRating`、`contentRatingDescription`、`offersIAP`、`inAppProductPrice` 等字段统一置为空字符串。
  - 将 `category` 映射到 metadata prompt 使用的 `genre/categories` 字段，保证 `GPTClient.analyze_app_metadata(...)` 仍可推断问卷类型。
  - 目的：让只有 `description/category` 的下载列表也能走 app metadata 分析，不再因为缺少旧字段或 `appId` 找不到而失败。

### 当前遗留问题

- `ui_cls.py` 的 UIED 分支仍需要进一步容错：当 UIED 工具返回空结果或内部抛出 `IndexError` 时，理想行为应该是只丢弃 UIED 结果，继续使用 OCR/YOLO 等其他工具结果，而不是让整个 `capture_and_process` 失败。
- XML 不可信页面仍可能存在视觉状态误归并风险；当前已通过更严格完整状态归并阈值降低概率，后续可以考虑结合 OCR 文本、可点击区域位置、局部截图相似度进一步增强判断。

## 2026-05-26

### XML 可信页面状态归并逻辑更新

- 本次改动主要处理 XML 可信页面中“只有局部动态文字变化，但实际仍是同一个页面”的状态误分裂问题。
- 原来的 XML 可信页面状态识别链路大致是：
  - `_capture_and_process(...)` 获取 XML 和截图；
  - 判断 `xml_reliable=True`；
  - 调用 `BaseUI.post_process_ui(...)` 得到处理后的 UI；
  - 调用 `compute_state_signature(...)` 生成 XML 状态签名；
  - `_resolve_state_identity(...)` 直接返回 `xml:<state_signature>`；
  - 结果是：只要动态文字导致 XML/state signature 变化，就可能生成新的 `state_sig`。
- 现在的 XML 可信页面状态识别链路调整为：
  - `_capture_and_process(...)` 获取 XML 和截图；
  - 判断 `xml_reliable=True`；
  - 调用 `BaseUI.post_process_ui(...)` 得到处理后的 UI；
  - 调用 `compute_state_signature(...)` 生成候选 XML 状态签名；
  - `_resolve_state_identity(..., uist=处理后的 UI)` 在创建新 `xml:<state_signature>` 前，先拿当前处理后的 UI 和已有 XML 可信状态做比较；
  - 如果能匹配到已有状态，则复用已有 `state_sig`；
  - 如果不能匹配到已有状态，才继续创建新的 `xml:<state_signature>`。
- 新增的同页归并判断逻辑基于处理后的 UI，而不是原始 XML：
  - 先比较 UI 树结构，包括节点 `id`、节点类型（`class/type`）和子节点层级；
  - 如果结构不能完全确认一致，再比较页面文本集合；
  - 当前文本容忍规则为：左侧独有文本数量不超过 4，且文本集合 Jaccard 相似度不低于 0.8；
  - 满足结构一致或文本容忍规则时，认为当前页面可以归并到已有状态。
- 影响：
  - 对于同一页面中局部滚动文字、推荐标签、动态标题等小范围文本变化，后续更倾向于复用已有 `state_sig`；
  - 也就是说，这类页面原本可能被分配成多个不同状态，现在会尽量归并到同一个状态地址；
  - 主流程中图结构、候选动作缓存、状态访问记录等也会随之使用归并后的已有 `state_sig`。
- 保持不变：
  - XML 不可信页面仍使用原有截图 pHash 相关逻辑；
  - 轻量级 drift 检测仍先按 XML hash / 截图 hash / pHash 做快速判断；
  - 如果轻量检测认为 XML 发生变化，会进入完整 capture，再由新的处理后 UI 归并逻辑决定是否复用已有状态。
- 已知限制：
  - 本次改动不解决截图未加载完成的问题；
  - 对于“XML/处理后 UI 基本一样，但截图从未加载完成变为加载完成”的场景，后续还需要单独做采样稳定性判断，例如等待重采样、连续 pHash 变化计数等。

### 调试脚本更新

- 更新 `test_debug/test_5.24/compare_xml_reliable_pages.py`：
  - 保留两页面结构、文本、截图 pHash 等相似度输出；
  - 新增基于处理后 UI 的同页判断输出；
  - 用于离线观察保存样本中的动态文本页面是否会被归并。

### 默认模型更新

- 将默认 LLM 模型从 `gpt-4o` 调整为 `gemini-2.5-flash`：
  - `main.py` 的 `--model` 默认值已调整；
  - `gpt_cls.py` 中 `GPTClient` 的默认 `model` 已调整。

## 2026-05-31

### Task Stack / 任务指导探索更新

- 新增 `task_manager.py`：
  - 引入 `Task` 和 `TaskManager`。
  - workflow 现在维护一个任务栈，用当前任务约束 navigation/router 的动作选择。
  - 初始任务为 `enter_main_page`，目标是进入稳定 APP 主界面。
  - LLM 可以在当前 UI 上提出子任务，workflow 会把子任务压入任务栈并优先执行。

- 合并版 navigation/router 输出扩展：
  - `NavigationRouterResult` 新增 `task_id`、`task_decision`、`proposed_tasks`。
  - `TaskDecision` 用于表示当前任务是否完成、失败或需要返回。
  - `ProposedTask` 用于表示 LLM 在当前 UI 上发现的新任务。
  - `ActionCandidate` 新增 `action_role`、`starts_task_type`、`starts_task_depth`。
  - `action_role=continue_current_task` 表示继续当前任务。
  - `action_role=start_child_task` 表示该动作会进入一个新子任务。

- 增加探索深度字段：
  - `exploration_depth=shallow`：轻度探索，只确认功能类型和基本相关性。
  - `exploration_depth=normal`：中度探索，允许进入关键入口。
  - `exploration_depth=deep`：重度探索，主要用于问卷/router 强相关任务。
  - 当前版本只把探索深度作为 prompt 指导，不直接改变任务步长预算。

- 调整任务步数字段：
  - 新任务状态不再主动输出 `remaining_steps`。
  - 现在使用 `step_budget` 表示任务总步数。
  - 使用 `used_steps` 表示任务已经使用的步数。
  - 任务耗尽判断改为 `used_steps >= step_budget`。
  - 可视化代码仍兼容旧 trace 中的 `remaining_steps`。

- workflow 集成：
  - 每次调度 `propose_navigation_and_router(...)` 时传入 `current_task`、`task_stack`、`utg_context` 和 `max_proposed_tasks`。
  - LLM 结果返回后，workflow 会根据 `proposed_tasks` 创建子任务。
  - 成功执行动作后统一更新 `used_steps`。
  - 任务状态会写入 `tasks.json` 和 `trace.jsonl`。
  - 默认 `workers` 调整为 1，减少并发结果和任务栈状态错位带来的调试干扰。

- trace / debug / HTML 输出更新：
  - `trace.jsonl` 的 `ctx.task` 中写入当前任务和任务栈。
  - 每个 UI 的 `debug_summary.md` 展示候选动作角色、子任务类型、子任务探索深度和 proposed tasks。
  - `visualize_run_interactive.py` 的节点详情页新增 Task 状态卡片。
  - HTML 候选动作表现在合并展示 LLM 字段和 `state_action_snapshot` 执行状态。
  - HTML timeline 的 Task 列显示 `task_id`、`task_type`、`exploration_depth` 和 `used_steps / step_budget`。

- 单 UI replay 和批量脚本更新：
  - `test_debug/run_llm_on_fetch_snap.py` 默认适配 `navigation_router` 模式，并构造 synthetic task context。
  - `test_debug/test_task_manager.py` 覆盖任务栈基础行为。
  - `test_debug/run_batch_apps.py` 默认 `workers=1`。
  - `test_debug/run_batch_apps.py` 新增 `--batch-id`。
  - 批量脚本读取 APP 列表时同时兼容 `APID` 和 `package` 列。

### 本轮验证

- `py_compile` 已通过。
- `test_debug/test_task_manager.py` 已通过，结果为 `5 passed`。
- 使用 Sudoku 重新运行验证：
  - trace: `traces/20260531_sudoku_depth_steps_schemafix_10min`
  - HTML: `traces/20260531_sudoku_depth_steps_schemafix_10min/index.html`
  - stop_reason: `strong_stall_timeout`
- 本次验证中曾发现 UniAPI/Gemini 不接受 response schema 中 enum 包含空字符串：
  - 报错字段为 `starts_task_depth.enum[0]: cannot be empty`。
  - 已将 `starts_task_depth` 改为普通字符串字段，并在返回后做 `shallow / normal / deep` 归一化。
  - 修复后不再出现该 schema 报错。

### 当前已知问题

- 缺少 UTG 上下文：
  - 当前 LLM 只看到当前 UI 和任务信息，不知道当前 UI 与父任务 UI、已知页面之间的图关系。
  - 因此 LLM 不一定能稳定给出正确的 `page_return_actions`。

- 返回动作不足会增加 fallback / restart：
  - 当 LLM 没有给出可用返回动作时，workflow 会退化到 fallback back 或 restart/replay。
  - 这不是 task stack 主逻辑错误，而是返回路径缺少图上下文导致的恢复问题。
  - restart 后如果找不到 replay path，系统只能从当前页面继续，而不一定能回到原任务页面。

- dismiss 逻辑尚未完全合并：
  - 当前仍保留独立 overlay dismiss / recovery 流程。
  - 后续应逐步并入 task-aware navigation/router，让 LLM 根据当前任务判断弹窗是否关闭、跳过或深入。

- task 调度仍是第一版：
  - 当前采用任务栈 DFS。
  - `priority` 和 `exploration_depth` 已记录，但还没有实现全局调度策略。
  - 后续可以结合 UTG、优先级和探索深度做任务切换。

### 下一阶段方向

- 将压缩后的 UTG 传给 LLM。
- 基于 UTG 改造返回动作策略，让 LLM 能看到当前 UI、父任务 UI 和已知返回路径。
- 减少 fallback back 和 restart 的使用频率。
- 将 dismiss 处理逐步并入 task-aware navigation/router。
- 根据问卷/router 问题整理任务类型和对应 prompt 规则。

### 2026-06-04 UTG 上下文与返回状态更新

- 文本版 UTG 接入主流程：
  - `state_graph.py` 增加 `TextUTGContext` 和文本 UTG 构造接口。
  - `workflow.py` 在调用 `propose_navigation_and_router(...)` 前为当前 UI 构造 `utg_context`。
  - 每个 UI 的 LLM 目录保存 `llm/utg_context.txt`。
  - `navigation_router_input.json` 中写入文本 UTG，上下文包含 `HOME`、`CURRENT`、`PARENT`、nodes、edges 和 home paths。

- `page_return_actions` 改为 UTG-aware：
  - prompt 要求 LLM 使用 `utg_context` 判断当前 UI 如何回到 home、parent 或 nearest useful ancestor。
  - 仍复用现有 `page_return_actions` 字段，不新增独立 `return_hints`。
  - 成功执行返回动作后，workflow 仍按真实执行结果记录 UTG 边。

- 新增页面返回状态字段：
  - `PageReturnStatus` 枚举包含 `has_visible_return`、`no_return_needed`、`needs_return_but_no_visible_control`。
  - `NavigationProposal` 新增 `page_return_status`，表示当前页面返回状态。
  - `NavigationProposal` 新增 `page_return_reason`，解释为什么选择该状态。
  - 该字段用于区分 `page_return_actions=[]` 是因为无需返回，还是因为需要返回但没找到可靠控件。

- trace / debug / HTML 输出更新：
  - `debug_summary.md` 展示 UTG context 文件路径、`page_return_status`、`page_return_reason` 和 `page_return_actions` 明细。
  - `visualize_run_interactive.py` 的 HTML 节点详情展示 `UTG Context`。
  - HTML 节点详情展示 `Page Return Actions` 表格和返回状态。
  - 当节点没有 LLM 返回或旧 trace 缺少字段时，HTML 仍兼容显示。

- 离线脚本与测试：
  - `test_debug/run_utg_return_eval.py` 复用 `state_graph.py` 的公共 UTG 文本构造接口。
  - 新增 `test_debug/test_utg_context.py`，覆盖文本 UTG 基础输出。

- 本轮验证：
  - `test_debug/test_utg_context.py`：`3 passed`。
  - Sudoku 5 分钟主流程验证：
    - trace: `traces/20260604_sudoku_return_status_5min_retry`
    - HTML: `traces/20260604_sudoku_return_status_5min_retry/index.html`
    - `time_budget_s=300`
    - `elapsed_seconds=303.184`
    - `stop_reason=time_budget_reached`
    - LLM calls: `16`
    - `propose_navigation_and_router`: `10`
    - `propose_blocks_fill`: `6`
  - `page_return_status` 分布：
    - `no_return_needed=4`
    - `has_visible_return=5`
    - `needs_return_but_no_visible_control=1`

- 当前保留问题：
  - task 调度仍是任务栈 DFS，暂未实现全局调度策略。
  - dismiss 逻辑仍保留独立 overlay dismiss / recovery 流程。
  - 如果后续发现动作语义不足，再考虑增加 `semantic_action`。
  - 如果文本 UTG 不足以覆盖复杂 case，再考虑 parent screenshot、parent UI digest 或 contact sheet。
