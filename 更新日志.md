# 更新日志

## 2026-05-04

### 本次代码功能更新（相对 `origin/ruogan..HEAD`）

- 重写 `LLM2` 导航系统提示词（`gpt_cls.py`）：
  - 从概念性说明升级为字段级/流程级约束；
  - 强化 `overlay_kind` 判定策略；
  - 强化 `candidate_actions` 与 `return_method/return_actions` 约束；
  - 目标是提升动作提议的稳定性与可执行性。
- LLM2判断页面是否有继续探索的必要。页面无探索必要就不继续分析。
- 修改paddle_cls代码，解决paddle版本适配问题。

## 2026-05-13

### capture_and_process 调试链路与三工具分支更新

- `ui_cls.py` 三工具后处理分支调整：
  - 新增 `BaseUI._assign_ids_all_nodes(uist)`，用于给 UI 树全部节点分配 `id` 并写入 `vid_map`。
  - `BaseUI.post_process_ui_three_tools_debug(...)` 改为使用全量 `id` 分配（`vid_map = _assign_ids_all_nodes(...)`）。
  - 目的：在 `xml_reliable=False` 的三工具调试场景中，方便完整核对融合后的全部元素。
  - 说明：`xml_reliable=True` 仍保持原有 `post_process_ui(...)` 逻辑，不受本次改动影响。

- `workflow.py` 调试捕获函数更新：
  - 新增/完善 `_capture_and_process_debug(self, timeout=..., debug=False, raw=None, xml_raw=None)`：
    - 支持注入本地 `raw/xml_raw` 做离线调试；
    - 保留与主链路一致的状态签名、缓存、裁剪、元信息打点流程；
    - 在调试函数内部按 `xml_reliable` 分流：
      - `True` -> `post_process_ui(...)`
      - `False` -> `post_process_ui_three_tools_debug(...)`
  - 修复调试入参判断逻辑，避免布尔与位运算混用导致的潜在异常。

- 新增本地离线调试脚本：
  - `mytest2/debug_capture_and_process.py`
  - 特点：
    - 使用代码内固定配置（图片/XML/输出目录），不依赖命令行参数；
    - 调用 `_capture_and_process_debug(...)`；
    - 输出 `snap/uist/vid_map/overlay/summary` 等调试产物；
    - 附加 `uist_total_nodes` 与 `vid_map_count` 对齐校验，便于确认全量映射是否生效。

### 当前接入状态

- 主流程 `_capture_and_process(...)` 已完全切换到与 `_capture_and_process_debug(...)` 相同的核心实现。
- 当前实际行为：
  - `xml_reliable=True`：仍走 `post_process_ui(...)`（原可信 XML 分支逻辑保持不变）。
  - `xml_reliable=False`：走 `post_process_ui_three_tools_debug(...)`（三工具融合 + 全量 `vid_map`）。

## 2026-05-16

### 调试链路阶段性更新

- *换用了新的api,注意接口url..

- `ui_cls.py` 三工具后处理分支更新：
  - 将 `BaseUI._POSTPROCESS_BUNDLE_VERSION` 从 `v1` 更新为 `v2`，避免复用旧 snapshot cache。
  - 新增 `BaseUI._force_all_nodes_clickable(uist)`：
    - 输入：三工具融合后的 `uist`。
    - 输出：原地修改节点，并返回修改节点数量。
    - 功能：把所有节点统一设置为 `clickable=True`、`enabled=True`。
  - 在 `BaseUI.post_process_ui_three_tools_debug(...)` 中，生成 `vid_map` 前先调用 `_force_all_nodes_clickable(...)`。
  - 当前效果：
    - `xml_reliable=False` 时，`snap["uist"]` 与 `snap["vid_map"]` 中所有节点都会被视作可点击。
    - `xml_reliable=True` 的可信 XML 分支不受影响。
  - 目的：避免三工具/OCR/UIED 生成的视觉按钮因为 `clickable=False` 被 LLM 导航阶段忽略。

- `gpt_cls.py` LLM Navigation 输出结构缩减：
  - 精简 `ActionStep`：
    - 保留 `action`、`element_id`、`text`、`priority`、`reasoning`。
    - 移除 `x`、`y`、`bbox`、`probe_grid`，不再允许 LLM 自行输出截图区域点击。
  - 精简 `ActionCandidate`：
    - 保留 `actions`、`score`。
    - 移除候选动作级 `return_method`、`return_actions`、`tags`。
  - 精简 `NavigationProposal`：
    - 保留 `state_sig`、`page_summary`、`overlay_kind`、`overlay_reason`、`overlay_dismiss_actions`、`candidate_actions`。
    - 新增 `page_return_actions`，用于表达页面级返回/关闭/切回动作。
    - 移除 `ui_view`、`ui_type`、`page_tags`、`tag_evidence`、`key_interactables`、`workflow_hints`、`exhausted*`、全局 `return_method/return_actions`、`why_these_actions`。
  - 同步修改 `_NAV_SYSTEM` 与 `_NAV_ROUTER_SYSTEM`：
    - 明确 `candidate_actions` 只放探索动作。
    - 明确 `page_return_actions` 只放页面级返回/关闭/切回动作。
    - 要求两者不要重复使用同一个 `element_id`。
    - 要求没有可见返回控件时 `page_return_actions=[]`，不编造返回动作。
    - 要求不输出坐标或 bbox。
  - 注意：当前精简后的 Navigation schema 主要服务调试脚本；完整主流程如果仍依赖旧字段，需要后续单独适配。

- `test_debug/test_llm_navigation.py` 与 `test_debug/test_llm_navigation_router.py` 更新：
  - 新增 `ui_digest.json` 输出：
    - `test_llm_navigation.py` 保存与 `GPTClient.propose_navigation(...)` 一致的 `_compact_digest(..., limit=240)`。
    - `test_llm_navigation_router.py` 保存与 `GPTClient.propose_navigation_and_router(...)` 一致的 `_compact_digest(..., limit=260)`。
    - 目的：方便核对 LLM 实际看到的 UI 元素、`clickable` 状态与标签内容。
  - 调整 LLM 动作 overlay 图：
    - 蓝色 `C*`：`candidate_actions`，表示探索候选动作。
    - 红色 `O*`：`overlay_dismiss_actions`，表示弹窗/遮挡层关闭动作。
    - 棕色/橙色 `PR*`：`page_return_actions`，表示页面级返回/关闭/切回动作。
  - 不再绘制旧的 `candidate.return_actions` 或全局 `return_actions`。
  - 删除测试脚本中基于 `bbox/x/y` 的画框 fallback；现在 overlay 只根据 `element_id -> vid_map` 定位。

## 2026-05-18

### 主流程 DFS / 回退 / 防循环更新

- `workflow.py` 主探索流程调整：
  - 引入页面级返回动作 `page_return_actions`，用于表达返回、关闭、切回父页面等页面级动作。
  - 候选动作 `candidate_actions` 仍只表示探索动作，不再和返回动作混用。
  - 页面普通候选动作执行后立即记录已尝试/已探索，避免“等待整条分支探索完才标记”导致重复点击。
  - 对已知状态优先复用状态缓存与已冻结动作，减少重复提交 LLM 与重复完整 `capture_and_process`。
  - 新增 return exhausted 相关处理：当页面候选动作耗尽、返回动作无效、UTG 父路径不可达、BACK 无变化时，将该状态标记为返回耗尽，避免重复执行同一套返回逻辑。
  - 新增 `recovery_route_exhausted` 终止保护：当 return exhausted 后尝试重启/replay 到全局 frontier 失败时，不再继续完整抓取同一页面，而是停止本次运行。

- 轻量级 drift / 已知状态处理调整：
  - 保留轻量 drift 作为动作执行后的优先判断路径。
  - 当轻量判断命中已知状态时，优先切换到已知状态缓存，而不是直接强制完整 `capture_and_process`。
  - 将完整状态归并阈值与轻量 drift 阈值拆开：完整状态归并更严格，降低 XML 不可信页面误归并风险。

### Overlay 处理更新

- `workflow.py` overlay dismiss 流程调整：
  - `overlay_kind=dismiss` 时优先执行 `overlay_dismiss_actions`。
  - 如果某个 dismiss 动作执行失败，继续尝试下一个 dismiss 动作。
  - 如果某个 dismiss 动作执行后页面无变化，也继续尝试下一个 dismiss 动作。

  - 当 LLM 错把明显的关闭/取消/稍后/跳过动作放到 `candidate_actions` 且 `overlay_dismiss_actions` 为空时，保留兼容 fallback，从候选动作中筛选可疑关闭动作。
  - 同一 overlay family 的 recovery 次数新增上限 `overlay_recovery_limit_per_family=3`。
  - 超过上限后设置 `overlay_recovery_exhausted`，主流程以 `overlay_recovery_exhausted` 作为停止原因结束，避免错误关闭按钮或不可关闭弹窗导致无限 recovery/restart。

- `gpt_cls.py` Navigation prompt 更新：
  - 明确 `overlay_kind=dismiss` 时 `candidate_actions=[]`。
  - 要求所有关闭、拒绝、稍后、跳过、继续类动作放入 `overlay_dismiss_actions`，并按安全程度排序。
  - 要求不要把全屏根节点或普通容器节点当作关闭按钮，除非它是唯一明确可点的关闭/继续控件。
  - 增加登录/社交登录约束：除非任务明确需要登录，或没有其他可探索的非登录入口，否则不要选择 login / sign-in / social-login 动作。
  - 合并版 `NavigationRouter` prompt 同步加入上述规则。

### 可视化与批量测试更新

- `main.py` 新增 `--auto-visualize-interactive`：
  - 主流程结束后可自动调用 `visualize_run_interactive.py` 生成交互式 UI 状态图。
  - 可视化失败只记录 warning，不影响主流程退出。

- `visualize_run_interactive.py` 交互式 HTML 更新：
  - 保留节点点击后查看节点详情的交互。
  - 右侧新增更适合人工调试的全局动作时间线。
  - 时间线重点展示状态发现、动作执行、状态跳转、外部跳转、recovery/restart 等关键事件。
  - 目标是结合左侧 UTG 图快速判断“在哪个状态执行了哪个动作，以及动作结果是什么”。

- `test_debug/run_batch_apps.py` 新增批量抽样测试脚本：
  - 从 `metadata_downloaded_apps.csv` 中按固定随机种子抽样。
  - 默认抽样 10 个 APP，每个 APP 运行 300 秒。
  - 根据 `category` 字段映射问卷类型：游戏类 -> `games`，社交/通信类 -> `social_apps`，其他 -> `others`。
  - 输出 `sampled_apps.csv`、`batch_summary.json`、`batch_summary.md`，便于快速检查批量运行是否出现死循环或严重错误。

### Metadata CSV 兼容更新

- `main.py` metadata 预分析兼容 `metadata_downloaded_apps.csv`：
  - 查找 APP 行时同时支持 `appId`、`APID`、`package`、`package_name`、`packageName`、`app_id`。
  - 字段归一化时保留旧格式字段，同时兼容新 CSV 中的 `description` 和 `category`。
  - 新 CSV 缺失的 `descriptionHTML`、`summary`、`contentRating`、`contentRatingDescription`、`offersIAP`、`inAppProductPrice` 等字段统一置为空字符串。
  - 将 `category` 映射到 metadata prompt 使用的 `genre/categories` 字段，保证 `GPTClient.analyze_app_metadata(...)` 仍可推断问卷类型。
  - 目的：让只有 `description/category` 的下载列表也能走 app metadata 分析，不再因为缺少旧字段或 `appId` 找不到而失败。

### 当前遗留问题

- `ui_cls.py` 的 UIED 分支仍需要进一步容错：当 UIED 工具返回空结果或内部抛出 `IndexError` 时，理想行为应该是只丢弃 UIED 结果，继续使用 OCR/YOLO 等其他工具结果，而不是让整个 `capture_and_process` 失败。
- XML 不可信页面仍可能存在视觉状态误归并风险；当前已通过更严格完整状态归并阈值降低概率，后续可以考虑结合 OCR 文本、可点击区域位置、局部截图相似度进一步增强判断。

## 2026-05-26

### XML 可信页面状态归并逻辑更新

- 本次改动主要处理 XML 可信页面中“只有局部动态文字变化，但实际仍是同一个页面”的状态误分裂问题。
- 原来的 XML 可信页面状态识别链路大致是：
  - `_capture_and_process(...)` 获取 XML 和截图；
  - 判断 `xml_reliable=True`；
  - 调用 `BaseUI.post_process_ui(...)` 得到处理后的 UI；
  - 调用 `compute_state_signature(...)` 生成 XML 状态签名；
  - `_resolve_state_identity(...)` 直接返回 `xml:<state_signature>`；
  - 结果是：只要动态文字导致 XML/state signature 变化，就可能生成新的 `state_sig`。
- 现在的 XML 可信页面状态识别链路调整为：
  - `_capture_and_process(...)` 获取 XML 和截图；
  - 判断 `xml_reliable=True`；
  - 调用 `BaseUI.post_process_ui(...)` 得到处理后的 UI；
  - 调用 `compute_state_signature(...)` 生成候选 XML 状态签名；
  - `_resolve_state_identity(..., uist=处理后的 UI)` 在创建新 `xml:<state_signature>` 前，先拿当前处理后的 UI 和已有 XML 可信状态做比较；
  - 如果能匹配到已有状态，则复用已有 `state_sig`；
  - 如果不能匹配到已有状态，才继续创建新的 `xml:<state_signature>`。
- 新增的同页归并判断逻辑基于处理后的 UI，而不是原始 XML：
  - 先比较 UI 树结构，包括节点 `id`、节点类型（`class/type`）和子节点层级；
  - 如果结构不能完全确认一致，再比较页面文本集合；
  - 当前文本容忍规则为：左侧独有文本数量不超过 4，且文本集合 Jaccard 相似度不低于 0.8；
  - 满足结构一致或文本容忍规则时，认为当前页面可以归并到已有状态。
- 影响：
  - 对于同一页面中局部滚动文字、推荐标签、动态标题等小范围文本变化，后续更倾向于复用已有 `state_sig`；
  - 也就是说，这类页面原本可能被分配成多个不同状态，现在会尽量归并到同一个状态地址；
  - 主流程中图结构、候选动作缓存、状态访问记录等也会随之使用归并后的已有 `state_sig`。
- 保持不变：
  - XML 不可信页面仍使用原有截图 pHash 相关逻辑；
  - 轻量级 drift 检测仍先按 XML hash / 截图 hash / pHash 做快速判断；
  - 如果轻量检测认为 XML 发生变化，会进入完整 capture，再由新的处理后 UI 归并逻辑决定是否复用已有状态。
- 已知限制：
  - 本次改动不解决截图未加载完成的问题；
  - 对于“XML/处理后 UI 基本一样，但截图从未加载完成变为加载完成”的场景，后续还需要单独做采样稳定性判断，例如等待重采样、连续 pHash 变化计数等。

### 调试脚本更新

- 更新 `test_debug/test_5.24/compare_xml_reliable_pages.py`：
  - 保留两页面结构、文本、截图 pHash 等相似度输出；
  - 新增基于处理后 UI 的同页判断输出；
  - 用于离线观察保存样本中的动态文本页面是否会被归并。

### 默认模型更新

- 将默认 LLM 模型从 `gpt-4o` 调整为 `gemini-2.5-flash`：
  - `main.py` 的 `--model` 默认值已调整；
  - `gpt_cls.py` 中 `GPTClient` 的默认 `model` 已调整。

## 2026-05-31

### Task Stack / 任务指导探索更新

- 新增 `task_manager.py`：
  - 引入 `Task` 和 `TaskManager`。
  - workflow 现在维护一个任务栈，用当前任务约束 navigation/router 的动作选择。
  - 初始任务为 `enter_main_page`，目标是进入稳定 APP 主界面。
  - LLM 可以在当前 UI 上提出子任务，workflow 会把子任务压入任务栈并优先执行。

- 合并版 navigation/router 输出扩展：
  - `NavigationRouterResult` 新增 `task_id`、`task_decision`、`proposed_tasks`。
  - `TaskDecision` 用于表示当前任务是否完成、失败或需要返回。
  - `ProposedTask` 用于表示 LLM 在当前 UI 上发现的新任务。
  - `ActionCandidate` 新增 `action_role`、`starts_task_type`、`starts_task_depth`。
  - `action_role=continue_current_task` 表示继续当前任务。
  - `action_role=start_child_task` 表示该动作会进入一个新子任务。

- 增加探索深度字段：
  - `exploration_depth=shallow`：轻度探索，只确认功能类型和基本相关性。
  - `exploration_depth=normal`：中度探索，允许进入关键入口。
  - `exploration_depth=deep`：重度探索，主要用于问卷/router 强相关任务。
  - 当前版本只把探索深度作为 prompt 指导，不直接改变任务步长预算。

- 调整任务步数字段：
  - 新任务状态不再主动输出 `remaining_steps`。
  - 现在使用 `step_budget` 表示任务总步数。
  - 使用 `used_steps` 表示任务已经使用的步数。
  - 任务耗尽判断改为 `used_steps >= step_budget`。
  - 可视化代码仍兼容旧 trace 中的 `remaining_steps`。

- workflow 集成：
  - 每次调度 `propose_navigation_and_router(...)` 时传入 `current_task`、`task_stack`、`utg_context` 和 `max_proposed_tasks`。
  - LLM 结果返回后，workflow 会根据 `proposed_tasks` 创建子任务。
  - 成功执行动作后统一更新 `used_steps`。
  - 任务状态会写入 `tasks.json` 和 `trace.jsonl`。
  - 默认 `workers` 调整为 1，减少并发结果和任务栈状态错位带来的调试干扰。

- trace / debug / HTML 输出更新：
  - `trace.jsonl` 的 `ctx.task` 中写入当前任务和任务栈。
  - 每个 UI 的 `debug_summary.md` 展示候选动作角色、子任务类型、子任务探索深度和 proposed tasks。
  - `visualize_run_interactive.py` 的节点详情页新增 Task 状态卡片。
  - HTML 候选动作表现在合并展示 LLM 字段和 `state_action_snapshot` 执行状态。
  - HTML timeline 的 Task 列显示 `task_id`、`task_type`、`exploration_depth` 和 `used_steps / step_budget`。

- 单 UI replay 和批量脚本更新：
  - `test_debug/run_llm_on_fetch_snap.py` 默认适配 `navigation_router` 模式，并构造 synthetic task context。
  - `test_debug/test_task_manager.py` 覆盖任务栈基础行为。
  - `test_debug/run_batch_apps.py` 默认 `workers=1`。
  - `test_debug/run_batch_apps.py` 新增 `--batch-id`。
  - 批量脚本读取 APP 列表时同时兼容 `APID` 和 `package` 列。

### 本轮验证

- `py_compile` 已通过。
- `test_debug/test_task_manager.py` 已通过，结果为 `5 passed`。
- 使用 Sudoku 重新运行验证：
  - trace: `traces/20260531_sudoku_depth_steps_schemafix_10min`
  - HTML: `traces/20260531_sudoku_depth_steps_schemafix_10min/index.html`
  - stop_reason: `strong_stall_timeout`
- 本次验证中曾发现 UniAPI/Gemini 不接受 response schema 中 enum 包含空字符串：
  - 报错字段为 `starts_task_depth.enum[0]: cannot be empty`。
  - 已将 `starts_task_depth` 改为普通字符串字段，并在返回后做 `shallow / normal / deep` 归一化。
  - 修复后不再出现该 schema 报错。

### 当前已知问题

- 缺少 UTG 上下文：
  - 当前 LLM 只看到当前 UI 和任务信息，不知道当前 UI 与父任务 UI、已知页面之间的图关系。
  - 因此 LLM 不一定能稳定给出正确的 `page_return_actions`。

- 返回动作不足会增加 fallback / restart：
  - 当 LLM 没有给出可用返回动作时，workflow 会退化到 fallback back 或 restart/replay。
  - 这不是 task stack 主逻辑错误，而是返回路径缺少图上下文导致的恢复问题。
  - restart 后如果找不到 replay path，系统只能从当前页面继续，而不一定能回到原任务页面。

- dismiss 逻辑尚未完全合并：
  - 当前仍保留独立 overlay dismiss / recovery 流程。
  - 后续应逐步并入 task-aware navigation/router，让 LLM 根据当前任务判断弹窗是否关闭、跳过或深入。

- task 调度仍是第一版：
  - 当前采用任务栈 DFS。
  - `priority` 和 `exploration_depth` 已记录，但还没有实现全局调度策略。
  - 后续可以结合 UTG、优先级和探索深度做任务切换。

### 下一阶段方向

- 将压缩后的 UTG 传给 LLM。
- 基于 UTG 改造返回动作策略，让 LLM 能看到当前 UI、父任务 UI 和已知返回路径。
- 减少 fallback back 和 restart 的使用频率。
- 将 dismiss 处理逐步并入 task-aware navigation/router。
- 根据问卷/router 问题整理任务类型和对应 prompt 规则。

### 2026-06-04 UTG 上下文与返回状态更新

- 文本版 UTG 接入主流程：
  - `state_graph.py` 增加 `TextUTGContext` 和文本 UTG 构造接口。
  - `workflow.py` 在调用 `propose_navigation_and_router(...)` 前为当前 UI 构造 `utg_context`。
  - 每个 UI 的 LLM 目录保存 `llm/utg_context.txt`。
  - `navigation_router_input.json` 中写入文本 UTG，上下文包含 `HOME`、`CURRENT`、`PARENT`、nodes、edges 和 home paths。

- `page_return_actions` 改为 UTG-aware：
  - prompt 要求 LLM 使用 `utg_context` 判断当前 UI 如何回到 home、parent 或 nearest useful ancestor。
  - 仍复用现有 `page_return_actions` 字段，不新增独立 `return_hints`。
  - 成功执行返回动作后，workflow 仍按真实执行结果记录 UTG 边。

- 新增页面返回状态字段：
  - `PageReturnStatus` 枚举包含 `has_visible_return`、`no_return_needed`、`needs_return_but_no_visible_control`。
  - `NavigationProposal` 新增 `page_return_status`，表示当前页面返回状态。
  - `NavigationProposal` 新增 `page_return_reason`，解释为什么选择该状态。
  - 该字段用于区分 `page_return_actions=[]` 是因为无需返回，还是因为需要返回但没找到可靠控件。

- trace / debug / HTML 输出更新：
  - `debug_summary.md` 展示 UTG context 文件路径、`page_return_status`、`page_return_reason` 和 `page_return_actions` 明细。
  - `visualize_run_interactive.py` 的 HTML 节点详情展示 `UTG Context`。
  - HTML 节点详情展示 `Page Return Actions` 表格和返回状态。
  - 当节点没有 LLM 返回或旧 trace 缺少字段时，HTML 仍兼容显示。

- 离线脚本与测试：
  - `test_debug/run_utg_return_eval.py` 复用 `state_graph.py` 的公共 UTG 文本构造接口。
  - 新增 `test_debug/test_utg_context.py`，覆盖文本 UTG 基础输出。

- 本轮验证：
  - `test_debug/test_utg_context.py`：`3 passed`。
  - Sudoku 5 分钟主流程验证：
    - trace: `traces/20260604_sudoku_return_status_5min_retry`
    - HTML: `traces/20260604_sudoku_return_status_5min_retry/index.html`
    - `time_budget_s=300`
    - `elapsed_seconds=303.184`
    - `stop_reason=time_budget_reached`
    - LLM calls: `16`
    - `propose_navigation_and_router`: `10`
    - `propose_blocks_fill`: `6`
  - `page_return_status` 分布：
    - `no_return_needed=4`
    - `has_visible_return=5`
    - `needs_return_but_no_visible_control=1`

- 当前保留问题：
  - task 调度仍是任务栈 DFS，暂未实现全局调度策略。
  - dismiss 逻辑仍保留独立 overlay dismiss / recovery 流程。
  - 如果后续发现动作语义不足，再考虑增加 `semantic_action`。
  - 如果文本 UTG 不足以覆盖复杂 case，再考虑 parent screenshot、parent UI digest 或 contact sheet。

### 2026-06-05 Page Kind 与 Dismiss 合并更新

- Dismiss 逻辑合并：
  - 删除旧的独立 `overlay_kind` / `overlay_dismiss_actions` 流程。
  - workflow 不再因为页面疑似弹窗而进入单独 dismiss 分支。
  - 稳定页、弹窗页、加载页统一走 `propose_navigation_and_router(...)` 返回的候选动作、返回动作和任务判断。
  - `page_kind` 只作为页面类型元信息，不直接决定 workflow 分支。

- Page Kind schema 更新：
  - `PageKind` 现在只包含 `stable`、`popup`、`loading`。
  - `NavigationProposal` 使用 `page_kind` 表示页面类型。
  - `NavigationProposal` 使用 `page_kind_reason` 解释页面类型判断原因。
  - 旧字段 `overlay_kind`、`overlay_reason`、`overlay_dismiss_actions` 不再保留兼容。

- workflow / StateGraph / trace 输出更新：
  - `workflow.py` 改为统一处理页面，不再优先执行 overlay dismiss action。
  - `state_graph.py` 节点记录 `page_kind`。
  - `trace_callbacks.py`、`debug_summary.md` 和 `timeline.md` 记录 `page_kind`。
  - 每个 UI 的 LLM 输出继续保留 `page_return_status`、`page_return_reason` 和 `page_return_actions`。

- HTML 可视化更新：
  - `visualize_run_interactive.py` 展示 `page_kind` 和 `page_kind_reason`。
  - HTML 不再展示旧的 overlay dismiss 字段。
  - 候选动作、返回动作和任务状态继续兼容显示。

- Prompt 调整：
  - `propose_navigation_and_router` 强调截图是主证据。
  - `ui_digest` / XML 只作为辅助信息，主要用于把截图里可见控件对齐到可执行的 `element_id`。
  - `xml_reliable=false` 时，UI 树结构和标签只能作为弱提示。
  - 对黑屏、白屏、主体区域未显示等页面，加入基于截图直觉判断的提示规则。
  - 当前 UI8 单步验证中，误动作明显减少，但 `page_kind` 仍可能被判成 `stable`，后续如果该问题频繁出现，需要考虑程序侧视觉异常特征。

- 调试脚本与测试：
  - `test_debug/run_llm_on_fetch_snap.py` 继续使用 `navigation_router` 模式进行单 UI replay。
  - 新增 `test_debug/test_page_kind_planning.py`，覆盖 `page_kind`、返回动作状态和候选动作角色的基础行为。
  - `test_debug/llm_replay_runs/` 已加入 `.gitignore`，作为单 UI replay 过程产物目录。

- 本轮验证：
  - `py_compile` 通过。
  - `test_debug/test_page_kind_planning.py` 和 `test_debug/test_utg_context.py` 通过。
  - Sudoku 5 分钟主流程验证：
    - trace: `traces/20260605_sudoku_page_kind_5min`
    - HTML: `traces/20260605_sudoku_page_kind_5min/index.html`
    - `time_budget_s=300`
    - `elapsed_seconds=331.317`
    - `stop_reason=time_budget_reached`
  - UI8 单步 replay 验证：
    - 输出目录: `test_debug/llm_replay_runs/20260605_ui8_page_kind_subjective/UI8`
    - `snap_count=1`
    - `candidate_actions=0`
    - `page_return_actions=0`
    - `router_updates=0`
    - `page_kind` 仍为 `stable`，但行为已收敛为不继续探索该页。

- 当前保留问题：
  - `page_kind=loading` 对黑屏、白屏、主体内容未显示的页面仍不稳定。
  - 当前 prompt 能减少误动作，但不能替代程序侧视觉异常检测。
  - task 调度仍是任务栈 DFS，暂未实现全局调度策略。
  - 任务类型还没有基于问卷/router 问题系统整理。
  - 不同任务类型的数量上限、完成质量判断和调度策略仍待设计。

- 下一阶段方向：
  - 根据问卷/router 问题整理任务类型。
  - 为不同任务类型设置探索策略、数量上限和完成质量判断。
  - 引入任务调度策略，不再只依赖简单 DFS。
  - 后续如仍频繁出现黑屏/白屏误判，再考虑增加程序侧视觉异常特征。

### 2026-06-06 任务类型与调度元数据更新

- 任务类型定义集中到 `task_manager.py`：
  - 新增 `TaskType` 枚举，第一版包含 `enter_main_page`、`explore_main_function`、`explore_payment`、`explore_policy`、`explore_settings`、`generic`。
  - 新增 `TaskTypeSpec` 和 `TASK_TYPE_SPECS`，记录每类任务的描述、完成目标、默认优先级和默认探索深度。
  - 新增 `normalize_task_type(...)`、`normalize_depth(...)`、`clamp_priority(...)`、`compose_task_priority(...)`、`task_type_prompt_rows(...)` 等辅助函数。
  - 未知或异常 `task_type` 会降级为 `generic`。

- 任务优先级元数据：
  - `Task` 新增 `type_priority`，表示任务类型默认重要性。
  - `Task` 新增 `llm_priority`，表示 LLM 针对当前具体入口给出的局部重要性。
  - `priority` 保留为最终调度分数，第一版公式为 `0.5 * type_priority + 0.5 * llm_priority`。
  - `tasks.json` 和 `task_stack` 上下文会输出这三个优先级字段。
  - 同一轮 LLM 返回多个 `proposed_tasks` 时，workflow 会先合成优先级，再按低到高压栈；由于当前任务栈是 LIFO，高优先级任务会先执行。

- LLM schema 与 prompt 更新：
  - `ProposedTask.task_type` 从自由字符串改为预定义 `TaskType`。
  - `propose_navigation_and_router(...)` 输入中新增 `allowed_task_types`，将任务类型表传给 LLM。
  - prompt 要求 `proposed_tasks[*].task_type` 必须来自 `allowed_task_types`，不再让 LLM 自行发明任务类型。
  - `explore_main_function` 不会因为任务完成自动打 tag；`home_like` 只由 LLM 在明确判断页面类似主页面或重要 section 入口时主动返回。

- 页面语义标签：
  - 新增 `PageTag` 枚举，第一版包含 `home`、`home_like`、`policy`、`payment`、`settings`。
  - `NavigationProposal` 新增 `page_tags`。
  - `StateGraph.Node` 新增 `page_tags`，并写入 `state_graph_snapshot.json`。
  - 文本 UTG 节点行会展示 `tags=...`，方便 LLM 使用已有图上下文。
  - `workflow.py` 会把 LLM 返回的 `navigation.page_tags` 写入当前图节点。
  - 任务完成时会根据明确任务类型补充 tag：
    - `enter_main_page` -> `home`
    - `explore_policy` -> `policy`
    - `explore_payment` -> `payment`
    - `explore_settings` -> `settings`
  - `explore_main_function` 和 `generic` 不自动补 tag。

- trace / HTML 输出更新：
  - `trace_callbacks.py` 的 `debug_summary.md` 展示 `page_tags`。
  - proposed task 输出展示 `priority`、`type_priority`、`llm_priority`。
  - `visualize_run_interactive.py` 的 HTML 节点详情展示 `page_tags`。
  - HTML 的 Task 状态和 Proposed Tasks 表格展示优先级来源字段。

- 本轮验证：
  - `test_debug/test_task_manager.py`、`test_debug/test_page_kind_planning.py`、`test_debug/test_utg_context.py` 通过，结果为 `17 passed`。
  - `py_compile` 通过，覆盖 `task_manager.py`、`gpt_cls.py`、`state_graph.py`、`workflow.py`、`trace_callbacks.py`、`visualize_run_interactive.py`、`test_debug/run_llm_on_fetch_snap.py`。
  - Sudoku 5 分钟主流程验证：
    - 有效 trace: `traces/20260606_sudoku_task_types_5min_v2`
    - HTML: `traces/20260606_sudoku_task_types_5min_v2/index.html`
    - `time_budget_s=300`
    - `elapsed_seconds=301.810`
    - `stop_reason=time_budget_reached`
    - LLM calls: `14`
    - `propose_navigation_and_router`: `10`
    - `propose_blocks_fill`: `4`
    - 动作数: `21`
    - UTG 节点数: `11`
    - UTG 边数: `18`

- 运行观察：
  - `tasks.json` 已能记录每个任务的 `task_type`、`status`、`priority`、`type_priority`、`llm_priority`、步数和结束原因。
  - 本次有效运行中任务类型分布：
    - `enter_main_page=1`
    - `explore_main_function=6`
    - `explore_payment=2`
    - `explore_settings=1`
  - 页面标签已写入图节点，例如主 hub 页面为 `home,home_like`，premium/loading 页面为 `payment`，settings 页面为 `settings`。

- 已修复问题：
  - 初次验证中发现 `TaskType` 枚举对象传入 `normalize_task_type(...)` 时会被误判为未知值，导致 `type_priority` 错误降级为 `generic` 的 `0.2`。
  - 已修复 `normalize_task_type(...)` 对 `TaskType` 枚举和 enum-like 对象的处理，并补充测试。
  - 因此 `traces/20260606_sudoku_task_types_5min` 只作为功能跑通参考；调度效果以 `traces/20260606_sudoku_task_types_5min_v2` 为准。

- 当前保留问题：
  - `tasks.json` 是最终任务快照，不是按任务聚合的完整时间线。
  - 因时间预算停止而未显式结束的任务仍显示为 `running`，语义上更接近“运行结束时仍未关闭”。
  - 当前还没有 `task_report.json` / `task_report.md` 这种任务视角报告。
  - 任务调度仍是 DFS 栈加本地优先级排序，尚未实现全局任务池、每类任务数量上限或 beam-search 风格切换策略。

### 2026-06-09 任务报告与任务调度第一版更新

- 任务视角报告：
  - 新增 `task_report.py`，在 run 结束时生成 `task_report.json` 和 `task_report.md`。
  - 报告按任务聚合 UI 观察、页面概要、任务进度、已选动作和动作意图，便于从任务视角回看执行过程。
  - `workflow.py` 在最终导出交互式 HTML 前调用任务报告生成逻辑。

- 离线任务分析与 block 命中报告：
  - 新增 `task_analysis_report.py`，离线读取 `tasks.json`、`trace.jsonl` 和各 UI 的 `navigation_router_result.json`，生成更完整的任务执行路径报告。
  - 新增 `task_router_brief_report.py`，生成任务到 UI、UI 到 `matched_block_ids` 的简略报告。
  - `matched_block_ids` 作为任务收益统计主依据，避免直接统计 `router_updates` 时被 `new_answer=no` 这类否定回答污染。
  - 报告中的 UI 显示改为 HTML/UTG 图编号优先，例如 `UI2 (UI000004)`：
    - `UI2` 是交互式 HTML 图里的节点编号。
    - `UI000004` 是本地 `states/` 目录编号。

- trace / HTML 输出补充：
  - `ActionCandidate` 增加 `action_intent`，用于描述候选动作服务当前任务或子任务的意图。
  - `NavigationRouterResult` 增加 `task_progress`，用于描述当前 UI 对当前任务的推进情况。
  - `workflow.py` 增加 `task_ui_observation` 和 `task_action_selected` 事件，写入任务执行过程。
  - `trace_callbacks.py` 和 `visualize_run_interactive.py` 展示 `task_progress`、`action_intent` 等任务相关信息。

- 任务调度第一版：
  - `TaskTypeSpec` 增加 `max_created`，表示某类任务在一次 APP 运行中最多创建多少个。
  - `TaskTypeSpec` 增加 `step_budget`，表示某类任务的每个任务最多执行多少步。
  - 当前配置：
    - `enter_main_page`: `max_created=1`, `step_budget=8`
    - `explore_main_function`: `max_created=8`, `step_budget=5`
    - `explore_payment`: `max_created=3`, `step_budget=3`
    - `explore_policy`: `max_created=3`, `step_budget=3`
    - `explore_settings`: `max_created=3`, `step_budget=3`
    - `generic`: `max_created=3`, `step_budget=4`
  - 创建 child task 时，真实 `step_budget` 由本地 `task_type` 配置决定，不再直接使用 LLM 返回的 `initial_steps`。
  - 如果某类任务已经达到 `max_created`，新任务不会进入任务栈，会记录到 `ignored_proposed_tasks`，并写入 `ignored_reason=max_created_per_type_exceeded`、`created_count`、`max_created` 等字段。

- 本轮验证：
  - 单元测试：
    - `test_debug/test_task_manager.py`: `14 passed`
    - 核心测试集合：`25 passed`
  - 语法检查：
    - `task_manager.py`
    - `workflow.py`
    - `task_router_brief_report.py`
  - 在线验证：
    - APP: `com.bd.nproject`
    - run: `traces/20260609_180918_com.bd.nproject`
    - HTML: `traces/20260609_180918_com.bd.nproject/index.html`
    - `exit=0`
    - `stop_reason=root_exhausted`
    - 实际耗时约 `242s`
    - LLM calls: `10`
    - total tokens: `190956`
  - 本次在线验证任务统计：
    - 总任务数 `4`
    - `ignored_proposed_tasks=0`
    - `enter_main_page`: 1 个，`step_budget=8`，`used_steps=4`
    - `explore_main_function`: 2 个，`step_budget=5`，均为 `expired`
    - `explore_settings`: 1 个，`step_budget=3`，`expired`
    - 由于本次任务总数较少，`max_created` 上限没有被触发，但 `step_budget` 已确认生效。

- 当前保留问题：
  - 简单 `max_created` 可能误杀后续更有价值的同类任务。例如某个 APP 有多个主要功能入口，但前几个入口已经占满同类任务数量。
  - 后续可以考虑“同类型未执行任务替换策略”：如果新任务优先级更高，并且已有同类型任务还没有消耗步数，则用新任务替换低优先级旧任务。
  - 当前 quota 只能控制数量，不能保证 APP 功能覆盖面。
  - 当前任务如果遇到多个并列入口，例如同一页面同时出现 `Terms of Service` 和 `Privacy Policy`，LLM 可能把多个入口都作为继续当前任务的候选动作；执行器实际只会选择其中一个动作执行，另一个入口可能依赖动作缓存和回退流程才能再次访问。后续应在 prompt / 任务创建规则中明确：多个独立证据入口应拆成多个子任务，而不是都作为当前任务的下一步动作。
  - 隐私政策或服务条款如果打开外部网页，当前外部页面保护逻辑可能会把流程拉回 APP，导致政策页内容无法采集。
  - 动态页面仍可能造成状态膨胀，导致任务步数快速耗尽。
  - 多设备在线时，`force_stop` 仍可能因为没有指定 adb serial 出现 `more than one device/emulator` warning；当前不影响 trace 生成，但后续应修复。
  - 因 run 结束未显式关闭的任务仍可能显示为 `running`，后续可整理为 `pending` / `interrupted` 等更准确终态。

- 后续改进思路：
  - 总结不同 APP 类型的人工探索模板，例如游戏类、社交类、内容/生活方式类分别应该如何覆盖主要功能、支付、设置、政策和互动入口。
  - 在确认 home 页面后，触发 home-level planner：让 LLM 结合 `app_metadata`、home 截图、UI digest、文本 UTG、问卷关注点和人工探索模板，先生成一组覆盖全面的高层探索任务。
  - 后续页面提出的新任务需要和 home-level planner 的总体计划去重或合并，避免任务集中在少数入口。
  - 当前第一版 quota/budget 作为基础护栏保留，后续 planner 用于提升覆盖面和任务规划质量。

### 2026-06-22 隐私政策 / 服务条款文档抓取流程更新

- Policy/TOS 文档抓取主流程：
  - 在 `workflow.py` 中接入隐私政策 / 服务条款文档采集分支。
  - 当当前任务为 `explore_policy`，并执行 policy/TOS 入口动作后，设置 `policy_capture_pending`。
  - 下一轮 capture 仍会正常生成 UI 状态并写入 UTG，但会优先进入 policy capture 分支，不再把 policy 文档页送交 navigation/router LLM 做普通探索。
  - policy 页面会在 UTG 节点上打 `policy` tag。

- 文档内容保存：
  - 新增 `traces/<run_id>/policy_captures/` 输出目录。
  - 每份文档按入口动作标题保存为独立目录，例如：
    - `policy_captures/Privacy Policy/`
    - `policy_captures/Terms of Service/`
    - 同名冲突时自动追加 `_2`、`_3`。
  - 每个文档目录保存：
    - `metadata.json`
    - `<document_title>.txt`
    - `screenshot.png`
  - 外部浏览器页面会额外尝试从 XML 中提取地址栏原始 URL，保存为 `url_raw`。
  - APP 内文档和外部浏览器文档第一版均通过点击正文区域后执行 Ctrl+A / Ctrl+C，从剪贴板读取全文。
  - 采集失败时会保存失败 metadata，便于后续排查。

- 任务和返回流程：
  - 采集成功后，当前 `explore_policy` 任务会标记为完成。
  - 采集失败时，当前 `explore_policy` 任务会标记为失败。
  - 采集后会尝试返回目标 APP：
    - 外部浏览器场景优先 `ensure_foreground(...)` 拉回目标 APP。
    - APP 内文档场景使用有限次数 back 返回。
    - 返回失败时进入 recovery 逻辑。

- trace / HTML / 报告展示：
  - `trace_callbacks.py` 增加 policy capture 相关事件记录。
  - `visualize_run_interactive.py` 在 HTML 节点详情中展示 policy capture 结果。
  - `task_report.py` 在任务报告中展示 policy capture 路径、文档标题、文本长度等信息。
  - 新增 `test_debug/run_policy_document_capture.py`，用于手动测试外部浏览器页和 APP 内 policy 文档页的文本抓取能力。

- 当前保留问题：
  - 前台包校验逻辑仍需要继续检查；部分情况下前台包不是目标 APP 时，流程仍可能继续执行普通分析。
  - 任务调度逻辑仍有待完善；多个 policy/TOS 入口是否都能访问，仍受任务创建、任务切换和动作绑定逻辑影响。

### 2026-07-01 APK/XAPK 安装与批量测试准备更新

- APK/XAPK 安装流程：
  - 新增 `device_utils.py` 中的 APK/XAPK 安装辅助逻辑，支持普通 APK、XAPK 解包、split APK 安装和 OBB 文件推送。
  - `main.py run` 增加运行前安装相关参数：
    - `--app-file`
    - `--install-before-run`
    - `--uninstall-before-install`
    - `--validate-launch-before-run`
    - `--uninstall-after-run`
    - `--install-timeout`
    - `--install-launch-wait-seconds`
    - `--install-temp-dir`
    - `--keep-install-temp`
    - `--adb-path`
  - 新增 `install-package` 和 `batch-install-smoke` 子命令，用于单个或批量安装/启动冒烟测试。
  - 安装预检结果保存为 `install_result.json`，便于定位安装失败、启动失败、前台包不匹配等问题。

- 设备控制修复：
  - `appium_android.py` 的 `force_stop(...)` 改为使用当前 `device_name` 指定 ADB 设备。
  - 该修改用于避免多设备在线时 `adb` 默认设备不明确导致关闭 APP 失败或操作到错误设备。

- 批量测试准备：
  - 新增 `test_debug/bench_run.py`，用于按 CSV 数据集顺序批量运行 APP。
  - 支持按 `start-index` / `limit` 分段运行，便于把 200 APP 或 2000 APP 数据集拆成多批测试。
  - 批量 runner 会生成 batch summary，并为每个 APP 调用主流程参数，包括安装、启动校验、运行后卸载、metadata CSV、自动 HTML 可视化等。

- 数据文件准备：
  - 新增 `APP_csv/known_dataset_200_metadata.csv`，用于 200 APP 小规模测试。
  - 新增 `APP_csv/minor_facing_dataset_2000_metadata.csv`，用于后续 2000 APP 大规模测试。

- metadata 输出：
  - `main.py` 增加 `metadata_result.json` 输出。
  - 该文件位于单次运行目录根部，例如 `traces/<run_id>/metadata_result.json`。
  - 当前只保存 metadata LLM 的输出结果，不保存输入 metadata。

- 当前保留问题：
  - metadata 分析仍沿用旧版部分字段和旧输出 schema，`questionnaire_type` 仍在 LLM 输出结构中；该字段后续将改为直接读取 CSV 的 `app_type`。
  - 在 UniAPI/Gemini 后端下，旧 schema 中 `questionnaire_type` 的空字符串 enum 会导致 structured parse 报错，再 fallback 到 JSON 解析；下一阶段会删除该 LLM 输出字段。
  - 前台包校验逻辑需要再次检查，尤其是隐私政策/服务条款页面抓取流程改动后，外部页面与目标 APP 前台校验之间可能存在冲突。

- 本轮验证：
  - `py_compile` 已覆盖 `device_utils.py`、`main.py`、`appium_android.py`。
  - 使用 `install-package` 对单个 APP 完成安装、安装校验和启动校验测试。
  - 使用外部 APK 目录进行 47 APP 安装方式预检，整体安装方式可用；少量失败主要来自 ADB daemon 或个别包卸载异常。

### 2026-07-01 metadata 分析流程修正与批量测试问题记录

- metadata 分析流程修正：
  - `gpt_cls.py` 中的 `AppMetadataSummary` 删除 `questionnaire_type` 字段。
  - metadata LLM 不再负责推断问卷类型，避免 UniAPI/Gemini structured schema 中空字符串 enum 导致 400 报错。
  - `main.py` 改为直接从 `APP_csv/known_dataset_200_metadata.csv` 的 `app_type` 字段读取问卷类型。
  - metadata 输入字段调整为当前 200 APP 数据集中的最终字段，重点包括：
    - `app_name`
    - `content_descriptors`
    - `age_rating_descriptors`
    - `category_name`
    - `category_code`
    - `details_full_description`
    - `application_category`
    - `data_safety_summary`
    - `security_practices_text`
    - `permissions_text`
    - `details_interactive_elements`
    - `details_in_app_purchases`
  - metadata LLM 输出继续保存为单次运行目录下的 `metadata_result.json`。
  - 新增 `test_debug/test_metadata_analysis.py`，用于单独按包名测试 metadata 分析结果。

- 数据文件：
  - 新增 `APP_csv/known_dataset_200.csv`，用于批量 runner 按包名和 APK 文件执行 200 APP 小规模测试。
  - 更新 `APP_csv/known_dataset_200_metadata.csv`，加入并使用 `app_type` 字段。

- Appium / ADB 稳定性修正：
  - `appium_android.py` 的 `init_connection(...)` 增加 ADB daemon 故障重试。
  - 当 Appium 创建 driver 时遇到 `could not read ok from ADB Server`、`failed to start daemon`、`cannot connect to daemon` 等错误，会自动执行一次 `adb kill-server` / `adb start-server`，然后重试创建 driver。
  - 该修改只针对明确的 ADB daemon 启动异常，不改变普通 Appium 配置错误的失败行为。

- 本轮 5 APP 小规模测试观察：
  - `com.google.android.apps.youtube.kids` 能进入探索，但在数学成人验证页暴露出结构化动作错误：LLM 的 `action_intent` 写明应输入正确答案，但 `actions` 数组中的数字点击序列不一致。
  - `com.pazugames.avatarworld` 和 `com.dramaton.slime` 属于启动或可用性异常场景，需要作为不可测 APP 处理。
  - `com.tocaboca.tocalifeworld` 能运行到时间预算，但动态首页导致状态膨胀，且关键开始按钮识别不稳定，实际探索质量不足。
  - `com.orange.kidspiano.music.songs` 安装和启动预检成功，但 Appium 创建 driver 时遇到 ADB daemon 异常，未进入 UI 探索；本次已增加 ADB 重启重试保护。

- 当前保留问题：
  - 动态页面仍缺少“同一页面 family”判断，页面内容轻微变化可能生成大量状态并消耗任务预算。
  - 后续可考虑在 drift 或 UTG replay 结果不一致时，把旧 UI、旧分析结果和新 UI 一起送给 LLM 判断是否属于同一状态 family；若属于同一 family，则沿用已有分析结果，否则将新 UI 作为新状态重新分析。
  - 数学验证、数字键盘等页面需要额外约束或本地校验，确保 LLM 的自然语言意图和结构化 `actions` 完全一致。
  - 前台包校验逻辑仍需继续检查，尤其是外部页面、Google Play 跳转和目标 APP 之间的切换场景。

### 2026-07-02 动态页面 family 判断与批量测试调整

- 动态页面 / 状态 family 判断：
  - `gpt_cls.py` 增加状态 family 对比相关 LLM 输出结构和 prompt。
  - `workflow.py` 接入两类状态对比：
    - 动作执行前 drift 对比：当准备执行旧 UI 上的动作、但当前截图已变化时，判断新旧 UI 是否仍可视为同一页面 family。
    - 状态转移后 mismatch 对比：沿 UTG/replay/return 后落到未知状态时，判断该未知状态是否其实是预期已知状态的动态变体。
  - 判断为同一 family 时，优先复用已有状态和分析结果；判断为不同状态时，将新 UI 作为新状态进入正常分析。
  - 目标是降低动态首页、动画页、内容轻微变化页面导致的状态膨胀。

- 离线调试脚本：
  - 新增 `test_debug/test_drift_before_action.py`，用于单独测试“动作执行前 drift 对比”。
  - 新增 `test_debug/test_transition_mismatch.py`，用于单独测试“状态转移后 mismatch 对比”。
  - 两个脚本均复用主流程中的截图/XML/UI digest 输入形式，便于用历史 trace 的两个 UI state 目录做离线验证。

- 批量测试调整：
  - `test_debug/bench_run.py` 默认不再传 `--validate-launch-before-run`。
  - `--validate-launch-before-run` 仍保留在 `main.py run` 中作为可选功能，但批量测试不再把“安装后前台包必须等于目标包”作为硬门槛。
  - 原因：50 APP 批量测试中发现大量 APP 安装成功后被 `foreground=com.android.vending` 误判为启动失败；单独 fresh 安装同一 APP 又能正常启动，说明该预检对批量实验过于敏感。

- DFS 对比准备：
  - 新增 DFS 分支补齐计划文档：`docs/superpowers/plans/2026-07-02-dfs-branch-update-plan-zh.md`。
  - 计划结论：以 `debug_5_28` 作为 DFS 基线，迁移 metadata、APK/XAPK 安装、UTG、page_kind、page_return_status、dynamic state family、policy capture、trace/html 等通用能力，但不迁移 TaskManager、任务类型、任务栈、任务 quota 和 task-bound action。

- 当前保留问题：
  - 安装预检失败或 Appium driver 创建失败时，cleanup 仍需要进一步统一；理想行为是只要传了 `--uninstall-after-run`，即使 workflow 没启动也应尝试卸载。
  - ADB/Appium 在长批量运行中仍可能进入 offline 或 daemon 异常状态；这类失败需要在 batch summary 中和 APP 自身不可运行区分开。
  - dynamic state family 当前只解决“是否同一页面 family”的第一版判断；对于持续动画页、广告弹窗、开始按钮识别不稳定等问题，还需要继续结合实际 trace 调整。
  - DFS 策略和任务调度策略的公平对比尚未开始；需要先按 DFS 补齐计划更新 `debug_5_28` 基线分支。

### 2026-07-03 OmniParser 外部视觉后处理接入记录

- 工具说明：
  - 引入 OmniParser 作为 XML 不可信页面的视觉后处理工具，用于在截图上识别更接近人眼看到的按钮、图标和可交互区域。
  - 当前版本没有把 OmniParser 正式合入主流程依赖环境，也没有做常驻模型加载；主流程只是通过外部工具目录进行引用。
  - 新增 `--visual-detector-backend omniparser_ocr` 配置，用于在 XML 失效或不可靠时切换到 OmniParser OCR/视觉解析路径。

- 当前效果：
  - 对 XML 不可信页面的处理能力明显增强，尤其是仅靠 UI XML 无法稳定识别按钮、链接或弹窗内容的页面。
  - 已补充 OmniParser 相关测试脚本，用于单独验证截图解析、overlay 输出和解析结果结构。
  - 已补充任务执行报告和 trace timing 相关调试信息，方便观察 UI 处理、LLM 调用、drift 判断等阶段的耗时。

- 当前代价：
  - 因为当前仍是外部引用/子进程调用方式，OmniParser 每处理一个未命中缓存的新 UI 都需要较重的启动和模型加载成本。
  - 实测 XML 不可信页面每一步 UI 解析大约需要 30 到 40 秒，整体探索速度明显下降。
  - 缓存命中时会快很多，但首次处理动态页面、广告页、弹窗页等新截图仍然会成为主要耗时来源。

- 后续方向：
  - 已记录 in-process 接入方案：后续计划把 OmniParser 封装为框架内的常驻模型加载模块，避免每次截图解析都冷启动外部进程。
  - 接入前会先写单独测试脚本验证在项目 `.venv` 中直接加载 OmniParser 模型是否可行，再决定是否替换当前外部引用实现。
